apiVersion: v1
kind: Service
metadata:
  name: calculator-service
  namespace: ai-devsecops

spec:
  selector:
    app: calculator

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000

  type: ClusterIP